<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Shared Folder - <%= folder.name %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
      <a class="navbar-brand" href="/">
        <i class="bi bi-cloud-arrow-up"></i> File Uploader
      </a>
    </div>
  </nav>

  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-10">
        <div class="card">
          <div class="card-body">
            <div class="d-flex justify-content-between align-items-center mb-4">
              <h2 class="card-title mb-0">
                <i class="bi bi-folder-fill text-warning"></i>
                <%= folder.name %>
              </h2>
              <span class="badge bg-info">
                <i class="bi bi-share"></i> Shared Folder
              </span>
            </div>

            <div class="alert alert-info">
              <i class="bi bi-clock"></i> 
              This link expires on: <strong><%= new Date(expiresAt).toLocaleString() %></strong>
            </div>

            <% if (files.length > 0) { %>
              <h5 class="mb-3">
                <i class="bi bi-file-earmark"></i> Files (<%= files.length %>)
              </h5>
              <div class="table-responsive">
                <table class="table table-hover">
                  <thead>
                    <tr>
                      <th>Name</th>
                      <th>Size</th>
                      <th>Type</th>
                      <th>Uploaded</th>
                      <th>Action</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% files.forEach(file => { %>
                      <tr>
                        <td>
                          <i class="bi bi-file-earmark-text text-primary"></i>
                          <%= file.name %>
                        </td>
                        <td><%= (file.size / 1024).toFixed(2) %> KB</td>
                        <td><%= file.mimeType %></td>
                        <td><%= new Date(file.createdAt).toLocaleDateString() %></td>
                        <td>
                          <a href="<%= file.url %>" target="_blank" class="btn btn-sm btn-primary">
                            <i class="bi bi-download"></i> Download
                          </a>
                        </td>
                      </tr>
                    <% }); %>
                  </tbody>
                </table>
              </div>
            <% } else { %>
              <div class="text-center py-5">
                <i class="bi bi-inbox" style="font-size: 4rem; color: #ccc;"></i>
                <p class="text-muted mt-3">This folder is empty</p>
              </div>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <footer class="bg-light py-3 mt-5">
    <div class="container text-center">
      <p class="mb-0 text-muted">&copy; 2025 File Uploader. All rights reserved.</p>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
